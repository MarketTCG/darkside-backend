export class Catalogue {
  set: string;
  number: string;
  name: string;
  type: string;
  aspects: any[]; // Define the structure of aspects if known
  traits: any[]; // Define the structure of traits if known
  arenas: any[]; // Define the structure of arenas if known
  cost: string;
  power: string;
  hp: string;
  frontText: string;
  doubleSided: boolean;
  rarity: string;
  unique: boolean;
  artist: string;
  frontArt: string;
  variantType: string;
  marketPrice: string;
 }
 